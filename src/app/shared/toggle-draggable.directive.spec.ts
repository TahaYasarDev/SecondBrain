import { ToggleDraggableDirective } from './toggle-draggable.directive';

describe('ToggleDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
